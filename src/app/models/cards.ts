export interface ServiceCards {
    title: string;
    description: string;
    icon: string;
    order: number;
}

export interface PortfolioCards {
    alt: string;
    image: string;
    tag: string;
    order: number;
}

export interface AboutCards {
    picture: string;
    name: string;
    description: string;
    socials: string[];
    order: number;
}